global:
  scrape_interval: 5s
  evaluation_interval: 5s




alerting:



rule_files:

scrape_configs:
- job_name: myapp
  static_configs:
  - targets:["myapp:8081"]


  # we dont need to specify metrics path beacuse it is default
  